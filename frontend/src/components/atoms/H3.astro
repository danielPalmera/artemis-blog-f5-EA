---
import { headingVariants, type HeadingVariants } from "../../utils/variants";

interface Props {
  type: HeadingVariants;
}

const { type = "footer" } = Astro.props as Props;
---

<h3 class={`heading-h2 text-(--color-text) ${headingVariants[type]}`}>
  Título
</h3>
