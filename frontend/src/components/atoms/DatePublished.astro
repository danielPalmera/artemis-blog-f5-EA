---
import { formatDate } from "../../utils/date";
interface Props {
  publishedAt: string;
}

const { publishedAt } = Astro.props;
---

<time class="caption uppercase tracking-wide" datetime={publishedAt}>
  {formatDate(publishedAt)}
</time>
