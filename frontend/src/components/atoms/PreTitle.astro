---
import { preTitleVariants, type PreTitleVariants } from "../../utils/variants";

interface Props {
  type: PreTitleVariants;
}
const { type = "hero" } = Astro.props;
---

<p
  class={`label uppercase text-(--color-accent-gold),  ${preTitleVariants[type]}`}
>
  <slot />
</p>
